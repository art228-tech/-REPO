# Ядро работает без сторонних зависимостей (только стандартная библиотека + ffmpeg/ffprobe в PATH).
# Распознавание речи нужно для субтитров и для поиска конца первого предложения.
faster-whisper>=1.0.0
