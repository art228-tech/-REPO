# Бот. Long polling: бот сам стучится в Telegram, входящих соединений нет,
# поэтому ни белый IP, ни домен, ни проброс портов не нужны.
aiogram>=3.13,<4

# Сторож нагрузки: читает загрузку процессора, чтобы поставить бота на паузу.
psutil>=5.9,<8

# Поддержки socks5 здесь намеренно нет: на части машин с Windows её установка
# срывается и уносит с собой всю остальную. Обычный прокси http работает без
# неё, а кому нужен именно socks — ставит отдельно: run.bat socks
