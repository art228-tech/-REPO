# Графический интерфейс собран на tkinter, который входит в стандартную
# поставку Python для Windows, поэтому ставить его отдельно не нужно.
requests>=2.31,<3

# Поддержка socks5-прокси в requests. Нужна там, где до api.elevenlabs.io
# не достучаться напрямую: большинство VPN поднимают локальный socks5.
PySocks>=1.7.1,<2

# Для запуска тестов:
#   pip install pytest
