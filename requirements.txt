# Бот. Long polling: бот сам стучится в Telegram, входящих соединений нет,
# поэтому ни белый IP, ни домен, ни проброс портов не нужны.
aiogram>=3.13,<4

# Сторож нагрузки: читает загрузку процессора, чтобы поставить бота на паузу.
psutil>=5.9,<8

# Прокси. Нужен для любого прокси, не только socks: aiogram без него отказывает
# и на обычном http. Если пакет когда-нибудь пропадёт из окружения, вернуть его
# можно командой  run.bat socks
aiohttp-socks>=0.8,<1
