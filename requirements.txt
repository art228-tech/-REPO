# Бот. Long polling: бот сам стучится в Telegram, входящих соединений нет,
# поэтому ни белый IP, ни домен, ни проброс портов не нужны.
aiogram>=3.13,<4

# Сторож нагрузки: читает загрузку процессора, чтобы поставить бота на паузу.
psutil>=5.9,<8

# Поддержки socks5 здесь намеренно нет. Пакет aiohttp-socks антивирусы Windows
# принимают за нежелательную программу и удаляют прямо во время установки, а
# вместе с ним обрывается и вся установка. Обычный прокси http работает без
# него, а кому нужен именно socks — ставится отдельно: run.bat socks
