# Отчёт по обработке текстов

## Архивы и средняя длина (символов / 1 текст)

| Архив | Вход | необыч avg | обыч avg |
|-------|------|------------|----------|
| исходный Drive | — | 286.6787 | 243.8611 |
| **v1** | исходный Drive + пайплайн | **268.7427** | **261.7971** |
| **v2 (этот)** | **v1 zip** + тот же пайплайн + `,` перед последним в `необыч` | **274.6354** | **255.9045** |

## Пайплайн (для v1 и v2 одинаковый)
1. 25 000 самых длинных в `необыч` → удалить последнее предложение
2. 25 000 самых коротких в `обыч` → добавить эти предложения
3. Поменять половины местами
4. Во всех предложениях `.` → `!`
5. *(только v2)* в `необыч`: `!` перед последним предложением → `,`

## Скачать v2
См. ссылку в ответе агента / артефакт `neobych_obych_processed_from_v1.zip`
