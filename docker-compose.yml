services:
  bot:
    build: .
    image: telegram-bot:latest
    container_name: telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot.sqlite3
    volumes:
      - bot-data:/data

volumes:
  bot-data:
