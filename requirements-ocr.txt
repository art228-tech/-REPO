# Поиск кнопок CapCut по ТЕКСТУ (OCR) — основной, надёжный способ навигации.
# Ставится ОТДЕЛЬНО и «мягко»: если не установится (например, под самую новую
# версию Python), приложение всё равно откроется и будет искать кнопки по
# картинкам-эталонам. Пакет rapidocr (>=3.0) совместим с Python 3.13.
#
# Офлайн, без внешних бинарников. Модели скачиваются автоматически при первом
# распознавании (нужен интернет один раз).
rapidocr>=3.0
