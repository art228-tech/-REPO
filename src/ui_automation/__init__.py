"""UI-автоматизация CapCut (Windows).

Часть шагов невозможно сделать правкой draft-файлов — только через интерфейс:
  * генерация автосубтитров (распознавание речи — онлайн-функция CapCut);
  * применение стиля субтитров без чёрных краёв (кнопкой) и шаблона (кнопкой);
  * экспорт (1080p / 60fps / битрейт «Выше») в папку «Результат».

Модуль будет реализован на pywinauto/pyautogui и откалиброван на машине
пользователя (русский интерфейс CapCut 8.7.0) итеративно по скриншотам и логам.
До этого — задокументированный интерфейс и заглушки.
"""

from __future__ import annotations


class UiAutomationNotReadyError(NotImplementedError):
    """UI-автоматизация ещё не настроена под конкретную сборку CapCut."""


def generate_captions_and_export() -> None:
    raise UiAutomationNotReadyError(
        "Автосубтитры и экспорт выполняются через интерфейс CapCut и требуют "
        "настройки на вашей машине (Windows, русский CapCut 9.0.0)."
    )
